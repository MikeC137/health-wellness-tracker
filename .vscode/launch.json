{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch HealthWellnessTracker (MSVC)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\bin\\HealthWellnessTracker.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "preLaunchTask": "Build HealthWellnessTracker"
        }
    ]
}